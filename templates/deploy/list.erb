*Latest deploys for <%= @repo.full_name %>*
<% @deploys.each do |deploy| %>
> <%= deploy.at %> - *<%= deploy.user %>* <<%= deploy.url %>|deployed> <%= @repo.repo_name %>/<%= deploy.ref %> to *<%= deploy.environment %>*
<% end %>
